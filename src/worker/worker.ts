import { Piece } from '../types'

export function processData(data: string): string {
  // Process the data without stalling the UI
  console.log(data)
  return data
}

export function quadraticSort(pieces: Piece[]) {
  console.log(pieces)
}
